# 新版更新檢查表

- 新PDF不得覆蓋舊PDF，建立新的version ID與獨立保存路徑。
- 重新計算兩份PDF的頁數、檔案大小與SHA-256。
- 重新驗證PDF實體頁碼及手冊印刷頁碼映射，不沿用舊版位移假設。
- 重新擷取PDF既有文字層，不使用OCR或AI重建。
- 依正式目錄重建完整目錄、貸款、函釋、FAQ、表單與附件索引。
- 重新判定複雜頁面並產生WebP原頁預覽。
- 重新建立瀏覽器端全文搜尋索引。
- 執行內容稽核、頁面呈現驗證、網站驗證及瀏覽器測試。
- 舊版本、舊PDF與舊網址永久保留。
