# 114年度 Beta.2.1 內容稽核紀錄

本輪以來源 PDF 的既有文字層重建函釋與書表資料狀態，未修改原始 PDF。

- 函釋來源索引僅接受可嚴格重解析的原始標頭、日期、完整文號、主旨起始與起始頁；狀態為 `source-indexed`，不是人工逐件確認。
- 函釋候選庫為自動偵測母集合，`promoted-to-source-index`、`duplicate-detection` 與 `pending-review` 互斥；僅最後一種列為真正待人工覆核。
- 書表候選庫同樣區分納入來源索引、排除與待覆核；候選總數不得表述為待覆核總數。
- 所有函釋結束頁目前標為 `start-only`，結束頁仍待人工確認。
- `printed-page-map.json` 逐列保留 359 個 PDF 頁位，PDF 第 1–2 頁為目錄前置頁，後續採印刷頁 = PDF 頁 − 2；42 個分散錨點已檢查，其餘非逐頁確認。

仍待人工覆核：逐件函釋涵蓋終點、函釋與書表的業務完整性，以及 359 頁全文的逐頁校讀。
