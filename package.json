{
  "name": "policy-agri-loan-handbook-tests",
  "private": true,
  "version": "114.0.0-beta.2.6.1",
  "description": "Development-only browser integration tests for the static handbook",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1"
  }
}
