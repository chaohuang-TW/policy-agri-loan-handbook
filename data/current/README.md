# Current official data layer

`official-updates.json` is the single system and business update track. `updates/disasters/` is an AFNA official gateway page, not a local announcement database. `coverage.json` records the global coverage conclusion and points to the per-source review log.

This layer is not a complete 115年度手冊. All records are source guides; users must return to the official source and lending institution.
